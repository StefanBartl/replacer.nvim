---@module 'plugin.replacer'
--- Lazy-friendly plugin entry: main config lives in replacer.init

-- Intentionally empty; prefer configuring via Lazy:
--   { dir = ".../replacer", main = "replacer", opts = { ... } }

-- If one prefers autoload without Lazy opts, uncomment:
-- pcall(require, "replacer") and require("replacer").setup({})

